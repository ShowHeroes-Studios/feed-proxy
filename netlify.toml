[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Optional: cleaner URL via redirect
[[redirects]]
  from = "/api/proxy"
  to = "/.netlify/functions/proxy"
  status = 200